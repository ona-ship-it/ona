export const dynamic = 'force-dynamic';
export const revalidate = 0;

import ViewProfileClient from '../view-profile/ViewProfileClient';

export default function ProfilePage() {
  return <ViewProfileClient />;
}